; RestaurantPhotoService::MAX_FILE_SIZE(5MB) 검증보다 PHP 기본 upload_max_filesize(2M)가
; 더 작아 3MB 파일도 업로드 단계에서 거부되는 문제(#59)를 막기 위한 서버 설정.
; PHP-FPM/CGI 환경에서 이 디렉터리(front controller 위치) 요청에 적용된다.
upload_max_filesize = 6M
post_max_size = 40M
